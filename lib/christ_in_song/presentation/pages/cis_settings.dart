import 'package:flutter/material.dart';

class CISSettings extends StatelessWidget {
  const CISSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Christ In Song Settings Screen',
        ),
      ),
    );
  }
}
